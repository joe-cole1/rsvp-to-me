services:
  app:
    build: .
    image: ghcr.io/joe-cole1/rsvp-to-me:latest
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      DATABASE_URL: file:/app/data/prod.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
